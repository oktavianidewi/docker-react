language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t oktavianidewi/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true oktavianidewi/docker-react npm run test
